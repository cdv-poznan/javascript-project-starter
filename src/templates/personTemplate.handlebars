<div class="container">
    <div class="row mt-3">
        <div class="col-12 col-md-auto  pt-3 text-center"><img class="person__photo "
                src="{{resultsPerson.apiImagesUrl}}/w185{{resultsPerson.profile_path}}"></div>
        <div class="col-12 col-md">
            <h1 class="my-2 person__title">{{resultsPerson.name}}</h1>
            <p>{{resultsPerson.biography}}</p>

        </div>
    </div>
    <h2 class="mt-5">Filmography</h2>
    {{> ./carouselTemplate.handlebars filmographyCarouselContext }}
</div>